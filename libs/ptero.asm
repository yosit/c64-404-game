// Pterodactyl module. Phase-0 stub — WP-C fills the body (sprite 1 flap
// frames, spawn/move logic, $d01e sprite-sprite collision → Game.Crash).
// Public interface (LOCKED): Ptero.Update, Ptero.Reset.
* = * "Ptero"
Ptero: {
	Update: {
			rts
	}

	Reset: {
			rts
	}
}
